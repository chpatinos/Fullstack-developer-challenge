version: 2.1
orbs:
  node: circleci/node@4.1.0

workflows:
  test_my_app:
    jobs:
      - test

jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - run:
          command: cd BACKEND && npm install && npm start && npm run test